import { approveMerchantApplication } from '@/admin/api/ApproveMerchantApplicationApi'
import { rejectMerchantApplication } from '@/admin/api/RejectMerchantApplicationApi'
import { resolveEligibilityReview } from '@/admin/api/ResolveEligibilityReviewApi'
import { resolveReviewAppeal } from '@/admin/api/ResolveReviewAppealApi'
import { resolveTicket } from '@/admin/api/ResolveTicketApi'

export {
  approveMerchantApplication,
  rejectMerchantApplication,
  resolveEligibilityReview,
  resolveReviewAppeal,
  resolveTicket,
}
